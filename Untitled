//2501测控高晓天
#include <stdio.h>
#include <stdlib.h>
int n;
int main()
{
    

    scanf("%d", &n);

    if (n <= 0)
    {
        printf("不存在\n");
        return 1;
    }

    int *arr = (int *)malloc(n * sizeof(int));
    if (!arr)
    {
        printf("内存分配失败\n");
        return 1;
    }

    for (int i = 0; i < n; i++)
    {
        arr[i] = i + 1;
    }

    while (1)
    {

        for (int i = 0; i < n; i++)
        {
            printf("%d ", arr[i]);
        }
        printf("\n");

        int i = n - 2;
        while (i >= 0 && arr[i] >= arr[i + 1])
        {
            i--;
        }

        if (i < 0)
        {
            break;
        }

        int j = n - 1;
        while (arr[j] <= arr[i])
        {
            j--;
        }

        int temp = arr[i];
        arr[i] = arr[j];
        arr[j] = temp;

        for (int l = i + 1, r = n - 1; l < r; l++, r--)
        {
            temp = arr[l];
            arr[l] = arr[r];
            arr[r] = temp;
        }
    }

    free(arr);
    return 0;
}
